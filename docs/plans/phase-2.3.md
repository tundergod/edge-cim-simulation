# Plan: Phase 2.3 — full validation + sensitivity + extrapolation + honesty labelling

Branch `phase-2.3` off `main`. TDD each step (write the gate/assert first, then make it pass). Constants:
AllCim L4 anchor stays byte-identical (0.1073/0.0649/0.0311, `validation/reports/phase2/e2e_l4.json`);
any runtime change must keep `abs(err − committed) < 1e-4` for 1B/3B/8B AND `plat.bw.eff_BW == 24.2`
exactly on the card+`mem_lpddr4x` path. Topology-alpha/edge, sensitivity, 13B/32GB, mixed-precision are
all `simulated`/`extrapolated`/`counterfactual` — never `validated`.

1. Wire the three topologies into the runtime (`simulator/runtime/{platform,runner,config}.py`). SINGLE SOURCE OF BANDWIDTH = the EXISTING `simulator/models/m2_memory.py::MemoryModel` topology resolver (do NOT build a second resolver): it already maps `cim_topo_card` → `dram_eff_BW_GBs` 24.2 and `cim_topo_edge` → `load_spec(mem_spec_ref).eff_BW_GBs` 33.3 × `noc_efficiency` 0.9 = 30.0, and exposes `floor_us`/`pcie_BW_GBs`. Today `Platform` bypasses it (builds `SharedBandwidth` straight from `memory_spec`, platform.py:33-41) — rewire `Platform` to source eff_BW (and the per-call floor) from `MemoryModel(load_spec(topology))` when a topology is given, keeping the non-topology `memory_spec` path for back-compat. Two gaps to close in `MemoryModel`: (i) `cim_topo_alpha` currently falls to `dram_eff_BW_GBs`=None (no on-card DRAM) → set its eff_BW = `pcie_BW_GBs` 3.9 (ALL DRAM-domain traffic streams from host over PCIe; state this modeling assumption); (ii) confirm card still yields the literal 24.2 so the L4 path stays byte-identical. `cfg.memory_spec` is a CONSISTENCY TAG asserted against the topology's own keys (single physics source = the topology spec), NOT a second BW feed: per-call floor is added to the TTFT (prefill) path ONLY — decode tok/s stays bandwidth-bound (D11: Card decode adds no per-call term; runner computes decode tok/s and TTFT from separate `run_dag` calls). Replace the `topology != cim_topo_card` `NotImplementedError` with: accept exactly {`cim_topo_card`,`cim_topo_alpha`,`cim_topo_edge`} fail-loud, reject any other at `runner.run()` AND in `SimConfig.validate()`. Define the topology×memory_spec CONTRACT fail-loud (today they are independent free fields — config.py:61-62 — so a conflicting combo would silently no-op): introduce a real SENTINEL default so "omitted" is distinguishable from "explicitly set" (today `from_dict` collapses both to `mem_lpddr4x` via `plat.get("memory_spec", _CAL_MEMORY)`, config.py:121 — undecidable). Change the default to a sentinel (`memory_spec=None` / `"topology_default"`) resolved per-topology: `cim_topo_card` resolves to / accepts `mem_lpddr4x` (reject any other); `cim_topo_edge` resolves to / accepts the spec's `mem_spec_ref` `mem_lpddr5` (reject any other); `cim_topo_alpha` has no on-card DRAM (eff_BW = `pcie_BW_GBs`, memory_spec does NOT feed the wall) so it accepts ONLY the sentinel and rejects any explicit LPDDR memory_spec as misleading. An explicit `memory_spec` that disagrees with the topology raises (no silent override) at both `SimConfig.validate()` and `runner.run()`. The existing L4 path sets `memory_spec="mem_lpddr4x"` explicitly with `topology=cim_topo_card` → still accepted, byte-identical. → verify: new `tests/test_topology.py` asserts each topology's resolved `eff_BW` (24.2/3.9/30.0) and floor (0/911.1/0), that the card+lpddr4x path yields `plat.bw.eff_BW == 24.2` exactly, that a topology×memory_spec mismatch raises at BOTH boundaries (cover: alpha+omitted OK; alpha+explicit `mem_lpddr4x` raises; edge+`mem_lpddr4x` raises; card+`mem_lpddr5` raises), and that an unknown topology raises at BOTH the config and run boundary; `validate_e2e_l4.py` 1B/3B/8B rel-errors stay within 1e-4 of 0.1073/0.0649/0.0311.
2. `validation/validate_topology_ab.py` — run the same decode workload (1B/3B/8B) across all three topologies via `runner.run`; emit a tok/s + per-decode-token data-movement (bytes / eff_BW) delta table; honesty label per row: card = MEASURED L4 anchor; alpha = SIMULATED **counterfactual** (Alpha hardware is LLM-INCAPABLE — closed firmware −1301, no on-card DRAM; the number is "if it could stream weights over PCIe, transport-bound at 3.9 GB/s"; PCIe transport itself is Phase-0.3-measured); edge = SIMULATED (`noc_efficiency` assumption, no edge silicon); record the D11 host-MMIO 911 µs as a per-call floor applied to TTFT only (explicit note). Add a residency-envelope note: topology A's 24.2 GB/s advantage holds ONLY while the model fits the 16 GiB on-card DRAM; on capacity overflow it would degrade toward the alpha host-PCIe wall (spill not modelled in v1 — fail-loud feasibility gate; tracked by #58). 1B/3B/8B all fit 16 GiB so the table itself is spill-free. → verify: `reports/phase2/topology_ab.json` exists with the 3×3 table; card rows REPRODUCE the committed mechanism predictions (`pred_tok_s` 14.4727/5.9659/2.7841) / rel_errors (0.1073/0.0649/0.0311) within 1e-4 — NOT equal to measured vendor tok/s (forcing pred==measured would be circular calibration); decode tok/s ordering alpha < card < edge (labelled a BY-CONSTRUCTION sanity check — it follows mechanically from the resolver's eff_BW constants 3.9<24.2<30.0, NOT a discovery; the REPORTED result is the magnitude of the alpha host-PCIe penalty incl. the 911 µs TTFT floor); the alpha row's label string contains the counterfactual/LLM-incapable disclaimer; every non-card row carries a `simulated` tag.
3. `validation/validate_sensitivity_l5.py` — split the sensitivity by the path on which each knob is actually TWO-SIDED (resources.py:35 `aggregate = min(k·eff_BW, knee·icn)`; AllCim serial is k=1 so `knee_GBs`/`interconnect_efficiency` only de-rate BELOW eff and are inert on the +20% side — sweeping them on the serial path would be half-by-construction). (a) AllCim serial tok/s sensitivity: ±20% on the eff_BW ANCHOR 24.2 (NOT on `bw_efficiency` directly). Since `eff = peak × bw_efficiency` (platform.py:38) with peak=34.1, set `bw_efficiency = (24.2 × {0.8, 1.0, 1.2}) / 34.1` so the endpoints' `memory_eff_BW_GBs` = 19.36 / 24.2 / 29.04 (genuinely two-sided — the default knee tracks eff so both directions move tok/s); decode-bandwidth-bound conclusion robustness lives here. (b) Contention-path sensitivity (k>1, SIMULATED — no concurrent silicon, #52): ±20% sweep of `knee_GBs`/`interconnect_efficiency` where they ARE two-sided, labelled simulated, reported as a contention-knee band (NOT counted toward the AllCim serial robustness). (c) Energy band: ±20% POST-PROCESS on runner's returned `energy_per_token_J` (runner already emits `energy_band_J`) — NOT a runner/M7 coeff sweep (Platform hardcodes `EnergyModel()`, platform.py:46; there is no energy-scale config hook). Do NOT sweep `concurrency_overlap_factor` (config.py:71 inert on the serial path). Report all bands as `[lo, hi]` (never a bare point); assert the qualitative conclusion (decode is bandwidth-bound — the memory-wall term dominates; topology ordering alpha < card < edge holds) is invariant across the AllCim `bw_efficiency` band. → verify: `reports/phase2/sensitivity.json` exists; `conclusion_robust == true` over the `bw_efficiency` band; every reported band is `[lo, hi]`; the AllCim serial sweep's endpoints have `memory_eff_BW_GBs` ≈ 19.36 / 24.2 / 29.04 (baseline pinned to 24.2) and tok/s actually moves both directions (a knob whose endpoints are equal is inert → removed); the knee/icn band is tagged `simulated`/contention-path and explicitly excluded from the serial robustness claim.
4. `validation/validate_holdout.py` — two honestly-distinct hold-out layers (the engine has NO size-coupled free parameter to refit, so name precisely what is and isn't fitted). (a) GENUINE leave-8B-out: the only free, size-regressed, fittable scalar is the CLOSED-FORM effective decode BW `fit_BW_GBs` (the two-pillar fit) — re-regress it on 1B + 3B ONLY and predict 8B in the closed form; this leaves the ENGINE'S 24.2 anchor UNTOUCHED (byte-identical, finding-2). (b) ENGINE path: the engine predicts 8B with NO 8B-specific tuning — the CIM-compute correction is parameter-free and the 24.2 backbone is a fixed scalar; report the engine's 8B rel-error as the mechanism hold-out, stating that the 24.2 backbone is PARTLY IN-SAMPLE (offline-regressed incl. 8B in Phase 1) so the genuinely out-of-sample content is the parameter-free compute correction. Gate (on the engine number): 8B rel-error ≤ 15% AND ≤ the committed closed-form 9.5%. If worse than 9.5% = Gate-6c → fix M1/M2/M3, NOT the integration layer, and name it in the PR. → verify: `reports/phase2/holdout.json` exists; names the {1B,3B}-refit scalar as `fit_BW_GBs` (NOT the fixed 24.2 anchor) and reports its leave-8B-out 8B delta; reports the engine 8B rel-error with the partly-in-sample caveat; `within_gate == true`; `not_worse_than_closed_form == true` (else exits non-zero and the PR documents the M-element moved).
5. 13B/32GB extrapolation (meta-trace through the SAME engine, ADR-0007) + mixed-precision limitation write-up. (a) Generate a 13B-class op inventory the device-INDEPENDENT way (ADR-0007: FakeTensor/meta trace, no real weights/GPU/Card — `tools/trace_export/op_inventory.py`): add the model's HF repo to `op_inventory.MODELS` and produce BOTH committed artifacts the runtime needs: (i) `measurements/op_inventory/<key>.json` (op×shape, via `op_inventory.py`) AND (ii) `traces/fixture/<key>.json.gz` (the value-flow trace-truth fixture the engine actually reads — `workload._load_structure`→`fixture_io.load_fixture`; generated by `trace_fixture.py`, which loops over `op_inventory.MODELS`, at the committed lengths PREFILL_FIX[256,1024]/DECODE_FIX[512,1024]). Update the hard-coded model lists to include the new key in `tests/test_trace_fixture.py`, `tests/test_workload.py`, AND `tests/test_dag_structure.py` (the structural-invariant oracle: softmax count, residual joins, embedding root) — or have `validate_extrapolation_13b.py` call `structural_check(model, phase, L)` directly. Also update `op_profile.py` `main()`'s hard-coded model list (op_profile.py:285) for consistency (not on the gate path — the gate instantiates `op_profile.Model(key)` directly — but keeps script-main and validator from diverging). Model source = Qwen2.5-14B (real, IN-FAMILY GQA — same op structure as the already-traced qwen2.5-7b, so M5/M3 prices it with no new op kinds; ~14B ≈ the "13B/32GB" point) [confirm at execution: `AutoConfig.from_pretrained` resolves with `HF_TOKEN`; else hand-write the config dims and label synthetic]. ALSO add the 14B architecture dims to `simulator/models/m4_cpu.py` `MODELS` (H/F/heads/hd/V, from the SAME HF config as the trace — CPU support pricing does `MODELS[model]`, m4_cpu.py:149, and KeyErrors otherwise; the dims MUST match the traced config, single source; alternative: have `CpuModel` read dims from `op_profile.Model(model).config`). NAMING DISCIPLINE (honesty): never label this point bare "13B" on any surface — it is Qwen2.5-14B (≈14B, one doubling beyond the 8B measured envelope, standing in for the ADR-0006 "~13B" stretch target). Mark the new `op_inventory.MODELS`/`m4_cpu.MODELS` entries with an inline `# EXTRAPOLATION ONLY (>8B, ADR-0006 stretch) — not a supported config` comment so they don't read as first-class in-scope models. Use the term "mechanistic-model extrapolation (engine path)" (ADR-0006:18), with `extrapolated` as the honesty tier (CONTEXT.md glossary). When this topology/13B wiring lands, append the crystallised decisions as short notes to ADR-0002 (memory_spec sentinel + topology eff_BW single-source via MemoryModel + host-fallback-on-overflow insight) and ADR-0006 (14B/32GB extrapolation policy + the validator-vs-runner capacity-formula divergence, xref #58). Cross-check completeness per ADR-0007 (traced ≡ analytical op enumeration), run `fixture_io.validate_value_flow`, and ensure `op_profile.Model` self-validation passes. Then run it through the FULL M5/M3 engine via `runner.run` on a hypothetical ≥32 GB SKU (so the fail-loud capacity gate passes — capacity stays a feasibility gate in 2.3, spill NOT modelled, decision locked), `topology=cim_topo_card`/`mem_lpddr4x`, with THREE distinct extrapolation flags: model-size (>8B measured), capacity (>16 GB envelope), AND `unit_shape_extrapolated` — count the per-op CIM shapes beyond M1's native measured envelope (`m1_cim_tile.is_extrapolated()`, flagged in `Platform.price` provenance: "EXTRAPOLATED beyond native envelope"; 14B FFN/lm_head shapes likely exceed it) as `cim_shape_extrapolated_count`, so the report states this is not merely "a bigger model" but also "some per-op shapes beyond the M1 native measured envelope (floor extrapolation)" → `reports/phase2/extrapolation_13b.json` (label: engine extrapolation via ADR-0007 meta-trace, NOT validated — no 14B silicon). REQUIRED computed capacity check with a PINNED formula (NOT prose, NOT a subjective flag) — all terms from the traced HF config, evaluated at `context`:
  · `weights_GB` = UNIQUE INT8 weight bytes / 1e9 (count each weight tensor once — not the per-op summed-with-count duplication the runner gate uses)
  · `kv_GB` = 2 · layers · kv_heads · head_dim · context · batch · 1 (INT8 KV) / 1e9
  · `act_GB` = worst-case (PREFILL) peak live activation = batch · context · hidden · 2 (fp16) / 1e9 (stated regime: prefill peak is the conservative resident reserve for a FEASIBILITY check; decode holds ~1 row and is far smaller)
  · `resident_footprint_GB` = weights_GB + kv_GB + act_GB
The validator emits `footprint_16GiB_margin_GB = 16 − resident_footprint_GB`, `footprint_32GiB_margin_GB = 32 − resident_footprint_GB`, and `capacity_risk_16GiB := footprint_16GiB_margin_GB < 0` (objective overflow at the evaluated context). NB the runner gate counts weights ONLY (runner.py:89) so this is a validator-side computed check; on overflow the real card spills to host LPDDR over PCIe (~3.9 GB/s) → decode collapse, which v1 does NOT model (capacity stays a fail-loud feasibility gate; tightening the runner gate to this full footprint + the spill behavior is issue #58). (b) Author a mixed-precision OUTPUT-QUALITY limitation section sourced from `validation/report_mixed_precision.py` / `validation/reports/phase2/mixed_precision.json`: v1 proves conversion-op COST only; output quality was NOT measured on target silicon (D3); cite literature; label as a limitation. → verify: a DEDICATED `validation/validate_extrapolation_13b.py` (part of the Step-7 hard gate) asserts the 13B-class op inventory AND `traces/fixture/<key>.json.gz` both exist; `op_profile.Model` self-validation + ADR-0007 completeness cross-check + `validate_value_flow` pass; the engine `runner.run` output is sane (positive tok/s, energy band); the structural-invariant `structural_check` passes for the 13B-class key; the updated model lists in `tests/test_trace_fixture.py`/`tests/test_workload.py`/`tests/test_dag_structure.py` pass; `extrapolation_13b.json` carries ALL THREE extrapolation flags (model-size, capacity, `unit_shape_extrapolated`) + `cim_shape_extrapolated_count`, the `footprint_16GiB_margin_GB`/`footprint_32GiB_margin_GB`/`capacity_risk_16GiB` computed values, and an `engine-extrapolation / not validated` label; the mixed-precision limitation section exists and contains no "validated mixed-precision" / 已驗證混合精度 language (grep clean).
6. Inject results into the report site. Three coupled edits: (i) add the new key→formatter→JSON-path entries to `tools/report/_metrics.py` `load()` (topology A/B/C deltas, sensitivity bands, hold-out 8B, 13B-class ENGINE extrapolation [key e.g. `extrap.qwen2_5_14b` / `13b_class`], mixed-precision limitation), each fail-loud if its JSON path is missing; (ii) add the `{{key}}` placeholders to the relevant `docs/report/phase1-site/src/*.src.html` page (extend `09-phase2-preview.src.html` into a Phase-2 RESULTS page, or add one); PLUS a dedicated **Phase-2 step-by-step walkthrough** page/section that narrates, in order, everything Phase 2 did across ALL waves — 2.1 (M5 workload/value-flow DAG + M3 event engine + M6 AllCim scheduler + L4 decode mechanism, smoke-vs-mechanism, memory-only ablation), 2.2a (value-flow fixture/DAG/oracle/memory-domains/provenance + pipeline knob), 2.2b (Scheduler ABC + conversion-op + CimHetero mixed-precision), 2.3 (topology A/B/C, sensitivity, hold-out, 13B-class engine extrapolation, mixed-precision limitation) — each step: what was done, why, which artifact/JSON backs it, and its honesty label, so a reader understands the whole phase one step at a time (numbers still `{{key}}`-injected from committed JSON); (iii) RECONCILE stale Phase-1-snapshot text: `09-phase2-preview.src.html` + `01-readiness.src.html` currently state M3/M6「未實作 / 只有 contract、沒有實作」which is now FALSE (M3/M6 shipped in 2.1) — update those lines to the implemented status OR clearly date/scope them as a frozen Phase-1 snapshot so a Phase-2 result page never presents「未實作」as current truth. → verify: `pytest tests/test_report_metrics.py` resolves every new key path; `docs/report/phase1-site/build.py --strict` exits 0 (an unresolved `{{key}}` fails the build via `_metrics.substitute`); a grep over the built Phase-2 result page shows no un-scoped「M3/M6 未實作」/「沒有實作」current-truth claim.
7. Gate 2.3 — every ADR-0006 system threshold reports PASS/FAIL + an honesty label, numbers trace to committed JSON, smoke-vs-mechanism and measured-vs-simulated-vs-counterfactual are explicit throughout. HARD gate (deterministic, must pass — CI/offline-safe): `.venv/bin/pytest tests/`, all six validators (`validate_e2e_l4`, `validate_topology_ab`, `validate_sensitivity_l5`, `validate_holdout`, `validate_contention`, `validate_extrapolation_13b`), `build.py --strict`, and L4 byte-identity against the committed artifact (1e-4). SEPARATE manual evidence (NOT a CI/PR-blocking gate — the user expects it this phase, but a board down for maintenance must not block the PR): re-confirm the L4 anchor against the live Card via a read-only subagent (`ssh metiscard`; vendor LLM re-measure protocol; do not modify the board), recorded as an appendix. → verify: pytest green; all validators print their gate PASS/FAIL; `build --strict` green; L4 byte-identical (1e-4) [hard]; live-Card re-confirmation logged as appendix evidence [manual, non-blocking unless the user demands a hardware re-measure].

Outputs:
- `simulator/runtime/{platform,runner,config}.py` (topology resolver + fail-loud), `tests/test_topology.py`
- `validation/validate_{topology_ab,sensitivity_l5,holdout,extrapolation_13b}.py`
- `validation/reports/phase2/{topology_ab,sensitivity,holdout,extrapolation_13b}.json`
- `measurements/op_inventory/<13B-class>.json` + `traces/fixture/<13B-class>.json.gz` (ADR-0007 meta-trace + value-flow fixture) + `tools/trace_export/op_inventory.py` MODELS entry + updated `tests/test_trace_fixture.py`/`tests/test_workload.py` model lists
- updated `tools/report/_metrics.py` + `docs/report/phase1-site/src/*.src.html` page(s) (Phase-2 results + mixed-precision limitation + the Phase-2 step-by-step walkthrough page covering 2.1/2.2a/2.2b/2.3)
- `docs/plans/phase-2.3.md`
