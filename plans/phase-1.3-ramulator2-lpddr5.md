# Plan: Phase 1.3 follow-up — Ramulator2 LPDDR5 single-stream BW cross-check (v2.1 API)

Context (not rationale): main-branch LPDDR5 throws `Failed to send refresh!`; the maintainer's fix is the **`v2.1` branch** (#58/#60/#89). **v2.1 is a different architecture** (verified): Python-bindings-only (nanobind module under `python/ramulator/`, **no CLI binary, no YAML**), `src/ramulator/` layout, codegen at build. It **computes `total_throughput_MBps` itself** (`src/ramulator/controller/controller_base.cpp:425`, using real `time_ps`) and ships a `tests/latency_throughput/` harness with `LatencyThroughputTrace` + `utils/spec.py::resolve_spec` (`max_theoretical_bw = channel_width*rate/8`, refresh-discounted `max_achievable_bw`). So we DRIVE v2.1 in-process and READ its throughput — **no hand-rolled tCK math**. Branch `phase-1.3`. Analytic LPDDR5 33.3 stays primary regardless.

1. **Switch to v2.1 + build the Python module.** Rewrite `tools/ramulator2/build.sh`: **pin the full SHA `278f1effc3838099a6ffe0ad5f9f572fea80c948`** — `git fetch --depth 1 origin <sha> && git checkout <sha>` (NOT `--branch v2.1`, which is only the moving tip — N6). Build: `mkdir build && cd build && cmake .. -DPython_EXECUTABLE=<abs>/.venv/bin/python && make -j` — the explicit `-DPython_EXECUTABLE` is **REQUIRED** (N5) so the `_ramulator` module + the `python -m ramulator codegen` target build against the venv, not system Python; the venv needs dev headers (`Development.Module`). Builds `libramulator.so` + the `python/ramulator/` extension + runs codegen (a CMake target, automatic). CMake≥3.14, Python≥3.10, nanobind auto-fetched. The main-branch patches (`param.h:91` `.as<T>`, cmake `<3.5` policy) are likely moot on v2.1 — grep-guard each. → verify (N4): `.venv/bin/python -c "import sys; R='tools/ramulator2/upstream'; sys.path[:0]=[R, R+'/python']; import ramulator; from tests.latency_throughput.utils import runner; print(ramulator.__file__)"` succeeds (BOTH the repo root — for the `tests` harness package — and `python/` on the path); `.venv` Python ≥3.10.
2. **Confirm a v2.1 LPDDR5 streaming run works (the abort is gone).** Mirror `tests/latency_throughput/testcases/*` + `utils/runner.py::run_simulation` for `{dram_class: LPDDR5, org_preset: LPDDR5_8Gb_x16, timing_preset: LPDDR5_6400, controller_class: LPDDR5}`, `streaming_only=True`. Use the row policy the v2.1 LPDDR5 testcase uses (Open by default per `runner.py`; ClosedRow only as an experiment, N2). → verify: `sim.run()` returns; NO "Failed to send refresh"; `sim.stats` contains `...total_throughput_MBps` > 0 and a non-zero served-request count; confirm `internal_prefetch_size == 16` in v2.1 `src/ramulator/dram/impl/LPDDR5.cpp`.
3. **Adapter `tools/analysis/mem_ramulator2.py`** (in-process; REUSE the v2.1 harness, do not re-implement). Import `tests.latency_throughput.utils.runner.run_simulation`, `...utils.spec.resolve_spec`, `...utils.checks.check_streaming_peak_bandwidth`/`check_streaming_bandwidth`, `tests.utils.extract_dram_layout`. cfg = `{name:"LPDDR5", dram_class:"LPDDR5", org_preset:"LPDDR5_8Gb_x16", timing_preset:"LPDDR5_6400", controller_class:"LPDDR5", stream_cls:64}`. `resolve_spec(cfg)` gives `channel_width` (= **16** for LPDDR5_8Gb_x16 — v2.1 fixed the main-branch `32` bug, #89/N1), `rate` (6400), `bytes_per_req`, `max_theoretical_bw` (= 16×6400/8/1000 = **12.8 GB/s** single channel), `max_achievable_bw` (refresh-discounted); prefetch (16, BL16) via `extract_dram_layout` or the DRAM class. Run the canonical streaming call TWICE (N2/N3): `run_simulation(cfg, nop_counter=1, read_ratio=100, num_probe_requests=0, num_streaming_requests=1_000_000, frontend_clock_ratio=4, warmup_cycles=10_000, streaming_only=True, refresh_enabled=False)` → **peak**; same with `refresh_enabled=True` → **achievable**. Get GB/s via `checks.py` (`stats["memory_system"]["controller"]["total_throughput_MBps"]/1000`). `efficiency = achievable_GBps / max_theoretical_bw`. SATURATION (B4): peak ≥ **0.95** of `max_theoretical_bw` (`check_streaming_peak_bandwidth` `deviation_from_theoretical_pct ≥ -5%`); if not, bump `frontend_clock_ratio` until throughput plateaus, assert plateau. Cross-check `efficiency` ≈ `max_achievable_bw/max_theoretical_bw` within a few %. Write `simulated/ramulator2/lpddr5_eff.json = {eff_BW_GBs: round(51.2*efficiency,1), efficiency, channel_width: 16, prefetch: 16, bytes_per_req, rate: 6400, peak_GBs_single_channel: 12.8, refresh_mode: "AllBank", v2_1_commit, honesty: "simulated (Ramulator2 v2.1 LPDDR5_6400 streaming), NOT silicon"}`. (51.2 = 4×12.8 at 6400 MT/s; LPDDR5 has no pseudochannel doubling → the single-channel efficiency transfers cleanly onto our 64-bit 51.2 peak.) → verify: peak efficiency ≥0.95 (saturated); achievable efficiency ∈ (0,1) AND agrees with v2.1's refresh-overhead estimate; JSON has all load-bearing fields (channel_width, prefetch, refresh_mode, peak, v2_1_commit).
4. **`engine='ramulator2'` live + drop-in verified.** Run `tools/analysis/check_phase1_3.py`. → verify: it hits the "Ramulator2 build present -> heavy path used" branch (NOT fallback); `MemoryModel(load_spec('mem_lpddr5'), engine='ramulator2').predict(wl)` returns the cached eff_BW with honesty "Ramulator2 ... NOT silicon"; `check_phase1_2.py` + recompose 9.5% still green.
5. **Delta report + figure + chapter.** `tools/analysis/build_mem_ramulator2.py`: the **Ramulator2 efficiency vs the analytic 0.65** (and eff_BW vs 33.3) → `validation/reports/phase1.3/m2_ramulator2.json` (mark: single-stream LPDDR5; multi-unit contention = Phase 2). `tools/plotting/mem_ramulator2_fig.py` → `docs/figures/phase1.3/M2-ramulator2.png` (Ramulator2 vs analytic BW + the two efficiencies). `docs/report/phase1.3/chapters/M-memory-ramulator2.md`. → verify: JSON has the analytic-vs-Ramulator2 delta + efficiency comparison; figure renders; chapter has sim-vs-analytic.
6. **Reconcile docs + commit.** Update `tools/ramulator2/build.sh` (v2.1 Python build, pinned SHA), `tools/ramulator2/README.md` (v2.1 API, refresh resolved, the actual efficiency result), retire `lpddr5.yaml`/`ddr4_smoke.yaml` (main-branch CLI — replace with a note pointing to the v2.1 driver), `docs/phase1.3-findings.md` (BW cross-check DONE + efficiency-vs-0.65), `docs/adr/0002-memory-model.md` (the Ramulator2 LPDDR5 efficiency result). secret-scan (`hf_…`); commit (exclude `papers/`, `upstream/` gitignored); push → PR #24. → verify: grep clean; check_phase1_2 / check_phase1_3 / recompose 9.5% green; pushed.

Fallback (no silent fudge): if v2.1's Python module won't build in `.venv` (nanobind/Python<3.10/Apple-clang issue), OR LPDDR5 streaming still aborts, OR the peak-saturation check (<0.95) / achievable-vs-refresh-overhead cross-check can't be reconciled → STOP, document the exact v2.1 result + the blocker, keep `engine='ramulator2'` in analytic fallback, report user. Do NOT fabricate a BW. Analytic 33.3 remains primary.

Outputs: v2.1 `build.sh` (pinned SHA, Python build); `simulated/ramulator2/lpddr5_eff.json`; `mem_ramulator2.py` (in-process driver, reusing v2.1 `utils/spec.py`) + `build_mem_ramulator2.py` + `mem_ramulator2_fig.py`; `validation/reports/phase1.3/m2_ramulator2.json`; `docs/figures/phase1.3/M2-ramulator2.png`; `docs/report/phase1.3/chapters/M-memory-ramulator2.md`; updated build.sh/README/findings/ADR-0002 (+ retired main-branch yaml); commit on `phase-1.3` → PR #24.
