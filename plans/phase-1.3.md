# Plan: Phase 1.3 — Wave 2：重型保真模擬引擎（ONNXim / Ramulator2）

範圍：把兩個 **C++ 重型 sim** 插進 Phase 1.2 已就緒的**共用引擎介面**（`Engine(spec, engine=).predict(workload)`），當**更高保真的可換引擎**：**ONNXim**（NPU）、**Ramulator2**（記憶體 DRAM）。皆標 `simulated, not silicon-validated`。**前提**：Phase 1.2 已 merge（spec 層 + 共用介面 + analytic 引擎齊備）。分支 `phase-1.3`。可平行（ONNXim ∥ Ramulator2）。

> **定位（grill 收斂）**：重型 sim 的招牌價值（Ramulator2 的多單元競爭；逐 token 整機）要 **Phase 2** 才被用到；本期角色 = (i) **交叉比對 Phase 1.2 的 analytic 單串流趨勢**、(ii) **介面就緒，Phase 2 一接就能用、silicon 一回來就能驗**。即時報酬偏低、真正報酬在 Phase 2——「先建好就緒」的取捨。**單點失敗（C++ build 爆）退 documented fallback、不影響已 merge 的 1.2。**

## 決策（已定）

- **D1 ONNXim（NPU heavy）**：generic systolic-NPU 模擬器配成 RKNPU2-approx（讀 `npu_rknpu2.json`，systolic 維借 Hexagon 32×32 標 borrowed）；當 NPU 的**模擬資料源** → 重 fit / cross-check Phase 1.2 的解析 roofline；HeteroInfer trend 仍交叉驗證。build 失敗 → OVERALL.md risk #7 fallback（解析-only / lookup-override）+ 回報 user。**非對 RKNPU2 silicon 驗證**（同 1.2 honesty）。
- **D2 Ramulator2（記憶體 heavy）**：接其內建 **LPDDR5**（subprocess + **代表性迭代 at 多個 KV 長度**（ADR-0002:128/512/1024，取 per-token-vs-KV 曲線）→ 有效 BW/latency → 解析外推；per-shape 快取）；驗 Phase 1.2 analytic 單串流 BW/latency。**LPDDR4/4X 的 C++ port = 後續**（`assumption`：Ramulator2 出貨 preset 疑只有 LPDDR5、無 LPDDR4——**build 時看 `src/dram/impl/` 確認**，非既定事實）。**SRAM 仍非 Ramulator2**（1.2 CACTI tier）。
- **D3 同介面**：兩者實作 1.2 定的 `Engine(spec, engine=).predict(workload) -> {latency_us,…}`（spec 建構綁、predict 只吃 workload），`engine='onnxim'|'ramulator2'` 與 1.2 `'analytic'` drop-in 互換；heavy 引擎封裝 adapter（shape→ONNX / bytes→trace）+ per-shape 快取。

---

## 0. 前置
1. **前置 gate**：確認 1.2 介面已 merge——`from simulator.models.m2_memory import MemoryModel; MemoryModel(load_spec('mem_lpddr5'), engine='analytic').predict(wl)` 成功（證明 `spec/engine=/predict` API 在、非 1.1 stub）；**API 不在 → STOP + 回報 user，不對 1.1 stub 建**。建 `tools/onnxim/`、`tools/ramulator2/`、`simulated/onnxim/`。 → verify：上述 import+predict 成功、目錄存在。

## A. ONNXim（NPU heavy；可與 B 平行）
2. ONNXim 取得+建置 `tools/onnxim/`（vendored/submodule）+ RKNPU2-approx config（讀 1.2 的 `npu_rknpu2.json`）。 → verify：build 成功 + smoke matmul；**失敗→記 risk#7 fallback + 回報 user**。
3. `tools/analysis/npu_onnxim_trace.py`：由 `measurements/op_inventory/` NPU shapes export ONNX → ONNXim 輸入（ADR-0007：export 次要、fallback=traced graph 直建）。 → verify：對 NPU shapes 產出輸入。
4. 跑 ONNXim → `simulated/onnxim/rknpu2_sim_matmul.json`（標 `simulated (generic-systolic, RKNPU2-approx), NOT silicon`；放 `simulated/` 非 `measurements/`，避免 sim 與 silicon 混）。 → verify：每 shape 有延遲+simulated 標。
5. **編輯 `simulator/models/m4_npu.py` 加 `engine='onnxim'` 分支**（dispatch 到 ONNXim adapter：封閉式對 ONNXim 表 fit + per-shape 快取）；與 1.2 `engine='analytic'` 同建構簽名、drop-in。 → verify：`NpuModel(spec, engine='onnxim').predict(wl)` 同 keys、復現 ONNXim 延遲（fit median/p95）、快取命中。
6. `build_m4_npu_onnxim.py`：**逐 shape ONNXim-vs-1.2-analytic delta** + HeteroInfer trend → `validation/reports/phase1.3/m4_npu_onnxim.json`（simulated；`upgrade=#13(silicon, 獨立、未滿足)`；ONNXim≠#13）。圖 `N3`(analytic vs ONNXim vs HeteroInfer trend)。章節 `chapters/N-npu-onnxim.md`。 → verify：JSON **含每 shape 的 analytic-vs-ONNXim delta 欄**（非只存在）+ 圖 + 章節 + #13 標 silicon-獨立。

## B. Ramulator2（記憶體 heavy；可與 A 平行）
7. Ramulator2 取得+建置 `tools/ramulator2/`（vendored/submodule）+ LPDDR5 config（對齊 1.2 `mem_lpddr5.json`）。 → verify：build 成功 + 對一 LPDDR5 trace 回有效 BW；**失敗→analytic 為主、回報 user**。
8. `tools/analysis/mem_ramulator2.py`：subprocess + 代表性迭代（一 decode/prefill 迭代 trace → 有效 BW/latency）+ per-shape 快取。 → verify：對代表 shape 回有效 BW/latency。
9. **編輯 `simulator/models/m2_memory.py` 加 `engine='ramulator2'` 分支**（dispatch 到 Ramulator2 adapter + per-shape 快取）；與 1.2 `engine='analytic'` 同建構簽名、drop-in。 → verify：`MemoryModel(spec, engine='ramulator2').predict(wl)` 同 keys、config 互換、快取命中。
10. `build_mem_ramulator2.py`：**逐 shape Ramulator2-vs-1.2-analytic（LPDDR5 單串流）delta** → `validation/reports/phase1.3/m2_ramulator2.json`（標：單串流 analytic 已夠準、Ramulator2 招牌競爭在 Phase 2）。圖 `M2-ramulator2`(Ramulator2 vs analytic)。章節 `chapters/M-memory-ramulator2.md`。 → verify：JSON **含 delta 欄** + 圖 + 章節。

## C. 合併
11. 交叉驗證：`engine='analytic'|'onnxim'|'ramulator2'` 三者 drop-in 互換、結果形狀一致、honesty 標一致。 → verify：`check_phase1_3.py` 跑通。
12. 報告 `build_phase1_3_report.py`（章節：ONNXim、Ramulator2、與 1.2 analytic 對照）→ HTML→PDF。`docs/phase1.3-findings.md`。 → verify：HTML/PDF、每章 sim-vs-analytic。
13. OVERALL.md（Phase 1.3 完成）、LOG.md。**reconcile「Ramulator2 階段」**：ADR-0002 加一行修訂（單串流 LPDDR5 cross-check 落 1.3、多單元競爭用 Phase 2）+ 修 OVERALL risk #6 措辭 + 標 `m2_memory.py` docstring 與 `docs/phase1.1-findings.md`（:19/:74/:130）、`docs/report/phase1.1/chapters/A2-m2-memory.md`（:90/:124）的「Ramulator2→Phase 2」為待更新（follow-up，grep 全掃）。secret-scan + commit（不動 papers/）+ `gh pr create` phase-1.3→main + 通知 user。 → verify：grep 無命中、PR 開出、ADR-0002/OVERALL/findings 不再與「Ramulator2 在 1.3」矛盾。

Outputs：`tools/onnxim/`+`tools/ramulator2/`（vendored）；`simulated/onnxim/rknpu2_sim_matmul.json`；heavy 引擎接 `m4_npu.py`/`m2_memory.py`（加 `engine='onnxim'|'ramulator2'` 分支）；scripts（npu_onnxim_trace/mem_ramulator2/build_*/check_phase1_3）；validation/reports/phase1.3/*.json；圖 N3/M2-ramulator2；報告 index.html+pdf+findings；OVERALL/LOG（含 ADR-0002 reconcile）；PR。


---

# 附：Phase 1.3 執行子計畫（已執行，併入此檔保留紀錄）

## 子計畫 A — Ramulator2 LPDDR5 single-stream（v2.1）


Context (not rationale): main-branch LPDDR5 throws `Failed to send refresh!`; the maintainer's fix is the **`v2.1` branch** (#58/#60/#89). **v2.1 is a different architecture** (verified): Python-bindings-only (nanobind module under `python/ramulator/`, **no CLI binary, no YAML**), `src/ramulator/` layout, codegen at build. It **computes `total_throughput_MBps` itself** (`src/ramulator/controller/controller_base.cpp:425`, using real `time_ps`) and ships a `tests/latency_throughput/` harness with `LatencyThroughputTrace` + `utils/spec.py::resolve_spec` (`max_theoretical_bw = channel_width*rate/8`, refresh-discounted `max_achievable_bw`). So we DRIVE v2.1 in-process and READ its throughput — **no hand-rolled tCK math**. Branch `phase-1.3`. Analytic LPDDR5 33.3 stays primary regardless.

1. **Switch to v2.1 + build the Python module.** Rewrite `tools/ramulator2/build.sh`: **pin the full SHA `278f1effc3838099a6ffe0ad5f9f572fea80c948`** — `git fetch --depth 1 origin <sha> && git checkout <sha>` (NOT `--branch v2.1`, which is only the moving tip — N6). Build: `mkdir build && cd build && cmake .. -DPython_EXECUTABLE=<abs>/.venv/bin/python && make -j` — the explicit `-DPython_EXECUTABLE` is **REQUIRED** (N5) so the `_ramulator` module + the `python -m ramulator codegen` target build against the venv, not system Python; the venv needs dev headers (`Development.Module`). Builds `libramulator.so` + the `python/ramulator/` extension + runs codegen (a CMake target, automatic). CMake≥3.14, Python≥3.10, nanobind auto-fetched. The main-branch patches (`param.h:91` `.as<T>`, cmake `<3.5` policy) are likely moot on v2.1 — grep-guard each. → verify (N4): `.venv/bin/python -c "import sys; R='tools/ramulator2/upstream'; sys.path[:0]=[R, R+'/python']; import ramulator; from tests.latency_throughput.utils import runner; print(ramulator.__file__)"` succeeds (BOTH the repo root — for the `tests` harness package — and `python/` on the path); `.venv` Python ≥3.10.
2. **Confirm a v2.1 LPDDR5 streaming run works (the abort is gone).** Mirror `tests/latency_throughput/testcases/*` + `utils/runner.py::run_simulation` for `{dram_class: LPDDR5, org_preset: LPDDR5_8Gb_x16, timing_preset: LPDDR5_6400, controller_class: LPDDR5}`, `streaming_only=True`. Use the row policy the v2.1 LPDDR5 testcase uses (Open by default per `runner.py`; ClosedRow only as an experiment, N2). → verify: `sim.run()` returns; NO "Failed to send refresh"; `sim.stats` contains `...total_throughput_MBps` > 0 and a non-zero served-request count; confirm `internal_prefetch_size == 16` in v2.1 `src/ramulator/dram/impl/LPDDR5.cpp`.
3. **Adapter `tools/analysis/mem_ramulator2.py`** (in-process; REUSE the v2.1 harness, do not re-implement). Import `tests.latency_throughput.utils.runner.run_simulation`, `...utils.spec.resolve_spec`, `...utils.checks.check_streaming_peak_bandwidth`/`check_streaming_bandwidth`, `tests.utils.extract_dram_layout`. cfg = `{name:"LPDDR5", dram_class:"LPDDR5", org_preset:"LPDDR5_8Gb_x16", timing_preset:"LPDDR5_6400", controller_class:"LPDDR5", stream_cls:64}`. `resolve_spec(cfg)` gives `channel_width` (= **16** for LPDDR5_8Gb_x16 — v2.1 fixed the main-branch `32` bug, #89/N1), `rate` (6400), `bytes_per_req`, `max_theoretical_bw` (= 16×6400/8/1000 = **12.8 GB/s** single channel), `max_achievable_bw` (refresh-discounted); prefetch (16, BL16) via `extract_dram_layout` or the DRAM class. Run the canonical streaming call TWICE (N2/N3): `run_simulation(cfg, nop_counter=1, read_ratio=100, num_probe_requests=0, num_streaming_requests=1_000_000, frontend_clock_ratio=4, warmup_cycles=10_000, streaming_only=True, refresh_enabled=False)` → **peak**; same with `refresh_enabled=True` → **achievable**. Get GB/s via `checks.py` (`stats["memory_system"]["controller"]["total_throughput_MBps"]/1000`). `efficiency = achievable_GBps / max_theoretical_bw`. SATURATION (B4): peak ≥ **0.95** of `max_theoretical_bw` (`check_streaming_peak_bandwidth` `deviation_from_theoretical_pct ≥ -5%`); if not, bump `frontend_clock_ratio` until throughput plateaus, assert plateau. Cross-check `efficiency` ≈ `max_achievable_bw/max_theoretical_bw` within a few %. Write `simulated/ramulator2/lpddr5_eff.json = {eff_BW_GBs: round(51.2*efficiency,1), efficiency, channel_width: 16, prefetch: 16, bytes_per_req, rate: 6400, peak_GBs_single_channel: 12.8, refresh_mode: "AllBank", v2_1_commit, honesty: "simulated (Ramulator2 v2.1 LPDDR5_6400 streaming), NOT silicon"}`. (51.2 = 4×12.8 at 6400 MT/s; LPDDR5 has no pseudochannel doubling → the single-channel efficiency transfers cleanly onto our 64-bit 51.2 peak.) → verify: peak efficiency ≥0.95 (saturated); achievable efficiency ∈ (0,1) AND agrees with v2.1's refresh-overhead estimate; JSON has all load-bearing fields (channel_width, prefetch, refresh_mode, peak, v2_1_commit).
4. **`engine='ramulator2'` live + drop-in verified.** Run `tools/analysis/check_phase1_3.py`. → verify: it hits the "Ramulator2 build present -> heavy path used" branch (NOT fallback); `MemoryModel(load_spec('mem_lpddr5'), engine='ramulator2').predict(wl)` returns the cached eff_BW with honesty "Ramulator2 ... NOT silicon"; `check_phase1_2.py` + recompose 9.5% still green.
5. **Delta report + figure + chapter.** `tools/analysis/build_mem_ramulator2.py`: the **Ramulator2 efficiency vs the analytic 0.65** (and eff_BW vs 33.3) → `validation/reports/phase1.3/m2_ramulator2.json` (mark: single-stream LPDDR5; multi-unit contention = Phase 2). `tools/plotting/mem_ramulator2_fig.py` → `docs/figures/phase1.3/M2-ramulator2.png` (Ramulator2 vs analytic BW + the two efficiencies). `docs/report/phase1.3/chapters/M-memory-ramulator2.md`. → verify: JSON has the analytic-vs-Ramulator2 delta + efficiency comparison; figure renders; chapter has sim-vs-analytic.
6. **Reconcile docs + commit.** Update `tools/ramulator2/build.sh` (v2.1 Python build, pinned SHA), `tools/ramulator2/README.md` (v2.1 API, refresh resolved, the actual efficiency result), retire `lpddr5.yaml`/`ddr4_smoke.yaml` (main-branch CLI — replace with a note pointing to the v2.1 driver), `docs/phase1.3-findings.md` (BW cross-check DONE + efficiency-vs-0.65), `docs/adr/0002-memory-model.md` (the Ramulator2 LPDDR5 efficiency result). secret-scan (`hf_…`); commit (exclude `papers/`, `upstream/` gitignored); push → PR #24. → verify: grep clean; check_phase1_2 / check_phase1_3 / recompose 9.5% green; pushed.

Fallback (no silent fudge): if v2.1's Python module won't build in `.venv` (nanobind/Python<3.10/Apple-clang issue), OR LPDDR5 streaming still aborts, OR the peak-saturation check (<0.95) / achievable-vs-refresh-overhead cross-check can't be reconciled → STOP, document the exact v2.1 result + the blocker, keep `engine='ramulator2'` in analytic fallback, report user. Do NOT fabricate a BW. Analytic 33.3 remains primary.

Outputs: v2.1 `build.sh` (pinned SHA, Python build); `simulated/ramulator2/lpddr5_eff.json`; `mem_ramulator2.py` (in-process driver, reusing v2.1 `utils/spec.py`) + `build_mem_ramulator2.py` + `mem_ramulator2_fig.py`; `validation/reports/phase1.3/m2_ramulator2.json`; `docs/figures/phase1.3/M2-ramulator2.png`; `docs/report/phase1.3/chapters/M-memory-ramulator2.md`; updated build.sh/README/findings/ADR-0002 (+ retired main-branch yaml); commit on `phase-1.3` → PR #24.

## 子計畫 B — ONNXim NPU heavy sim（Docker on metiscard）


Context (not rationale): ONNXim (PSAL-POSTECH, pinned commit `a1e86296`) is Ubuntu-20.04/gcc-10/conan-1.57 only — won't build on macOS. Confirmed `metiscard` (x86 Ubuntu 24.04, **Docker 28.1.1**) suitable: build+run ONNXim in its own `ubuntu:20.04` Dockerfile container, rsync results back. ONNXim = generic systolic-NPU sim configured RKNPU2-approx; **simulated, NOT silicon** (ONNXim ≠ issue #13; #13 stays superseded). Lower value than Ramulator2 (sim-vs-sim vs the already-`simulated` analytic NPU). Branch `phase-1.3`. Analytic NPU stays the deliverable; honest fallback on any failure.

Key mechanics (verified in the pinned source — the README is stale): the binary is `./build/bin/Simulator --config <hw> --models_list <list>` (flag is **`--models_list`**, NOT `--model`; strict boost parse → `--model` crashes). The Dockerfile `COPY ./ ONNXim` + `ENV ONNXIM_HOME=/workspace/ONNXim`; the built binary, `configs/`, `models/`, `model_lists/` ALL live at **`/workspace/ONNXim`** and `main.cc` reads `$ONNXIM_HOME/models/{name}/{name}.onnx`. Single GEMM is first-class (`scripts/generate_matmul_onnx.py`) but its shapes are a **hardcoded `size_list` (line 8, no args)**. `Simulator.cc:203` prints `Simulation Finished at {cycle} cycle {us} us` (latency directly). `.mapping` files are optional (empty → internal tiling).

1. **Build ONNXim in Docker on metiscard (feasibility gate).** `ssh metiscard`; `git clone https://github.com/PSAL-POSTECH/ONNXim onnxim && cd onnxim && git checkout a1e86296 && git submodule update --recursive --init`; `docker build . -t onnxim`. Smoke — run INSIDE the image tree (do NOT bind-mount over `/workspace`, that shadows the in-image build): `docker run --rm onnxim bash -lc 'cd $ONNXIM_HOME && ./build/bin/Simulator --config ./configs/systolic_ws_8x8_c4_simple_noc_transformer.json --models_list ./example/models_list.json'` (use `models_list.json` = resnet18, default mode, whose onnx IS shipped — NOT `language_models.json`, which needs `--mode language` + an unshipped onnx). → verify: image builds AND the smoke prints `Simulation Finished at … us` (NOT `Ramulator::InitializationError` — issue #32). If ramulator2 crashes, the RKNPU2-approx config (step 2) uses `dram_type: simple` (`SimpleDram`, needs no external config; carries an upstream `FIXME: bugs` note — acceptable, same family the analytic config would use). FAIL (build / submodule / both DRAM backends unrunnable) → documented fallback + report user, STOP.
2. **RKNPU2-approx HW config** `tools/onnxim/rknpu2_approx.json` (committed). Copy the WHOLE `systolic_ws_8x8_c4_simple_noc_transformer.json` (keep ALL mandatory top-level fields — `num_cores, core_freq, core_print_interval, dram_freq, dram_channels, icnt_type, icnt_freq, scheduler, precision, layout` — none have defaults), then EDIT: `num_cores: 3` + **three** (not four) full per-core `core_config.core_0/1/2` blocks (each keeps all ~17 fields), set per core `core_type: systolic_ws`, `core_width: 32`, `core_height: 32` (borrowed Hexagon 32×32; `gemm_mapping` asserts width==height — 32×32 OK), `num_cores: 3`, **`precision: 1`** (INT8 byte-width — drives SRAM/DRAM traffic; the 8x8 configs default to 2/FP16; note ONNXim's peak-ops formula is precision-agnostic so this is a traffic, not a peak-TOPS, change), `core_freq: 1000` (MHz → 32·32·1000·2·3/1000 = 6144 GFLOPS ≈ **6.14 TOPS** ≈ datasheet 6), runnable `dram_type` (per step-1 smoke). Provenance: borrowed 32×32 + datasheet 6 TOPS = simulated, NOT silicon. → verify: `Simulator --config rknpu2_approx.json --models_list <a matmul list>` prints `Simulation Finished at … us`.
3. **Matmul ONNX + run per shape — driver `tools/analysis/npu_onnxim_trace.py` (runs on metiscard, orchestrates `docker run`).** Define ONE explicit canonical `(M,K,N)` list (NPU LLM GEMM: q/o=(M,4096,4096), kv=(M,4096,1024), gate/up=(M,4096,14336), down=(M,14336,4096) at M∈{1,256}; + staircase K=2048,N∈{64,128,256,512,1024,1536,2048,3072} for the HeteroInfer trend). Because each `docker run --rm` is a FRESH container (anything cp'd/sed'd into a prior `--rm` container is gone) and `generate_matmul_onnx.py`'s `size_list` is hardcoded (no args), step 3 is ONE long-lived `docker run --rm onnxim bash -lc '<script>'` whose single script: (i) heredoc-writes the committed `rknpu2_approx.json` to `$ONNXIM_HOME/configs/rknpu2_approx.json`, then (ii) loops the canonical shapes — per shape `sed -i 's/^size_list = .*/size_list = [[M,K,N]]/' $ONNXIM_HOME/scripts/generate_matmul_onnx.py` (the `^size_list` anchor hits the active line 8, NOT the commented lines 6-7), `python3 scripts/generate_matmul_onnx.py` (writes `$ONNXIM_HOME/models/matmul_M_K_N/…onnx` + `model_lists/matmul_M_K_N.json`), `./build/bin/Simulator --config configs/rknpu2_approx.json --models_list model_lists/matmul_M_K_N.json`, echo `SHAPE M K N <us>` parsed from `Simulation Finished at …`. (NO `docker cp` — it doesn't fit `--rm`; the config + sed + loop all live in the one container's script.) Capture stdout on metiscard. → verify: a `latency_us` for every shape in the list (no shape missing).
4. **Collect → `simulated/onnxim/rknpu2_sim_matmul.json`** (driver writes on metiscard; rsync back). Schema (`m4_npu.py` keys `(M,K,N)` exactly): `{"rows":[{"shape":[M,K,N],"latency_us":…},…], "config":"rknpu2_approx (32x32 x3, INT8, dram=<simple|ramulator2>)", "onnxim_commit":"a1e86296", "honesty":"simulated (ONNXim generic-systolic, RKNPU2-approx), NOT silicon"}`. → verify: a latency per shape; honesty tag; under `simulated/`.
5. **`engine='onnxim'` live.** Run `tools/analysis/check_phase1_3.py`. → verify: hits "ONNXim build present -> heavy path" (NOT fallback); `NpuModel(load_spec('npu_rknpu2'), engine='onnxim').predict(Workload(op='matmul', M,K,N))` for an in-list shape returns the ONNXim latency (exact-tuple match), honesty "ONNXim … NOT silicon"; `check_phase1_2` + recompose 9.5% green.
6. **Delta report + figure + chapter.** `tools/analysis/build_m4_npu_onnxim.py`: the SAME canonical `(M,K,N)` list is the single source for BOTH ONNXim and `NpuModel(engine='analytic').predict` (N4). **Assert each shape is an ONNXim HIT** (predict provenance contains "ONNXim … NOT silicon", not "fallback") BEFORE computing its delta (NB-3) — a miss is a hard error, never a silent analytic-vs-analytic compare. Per-shape ONNXim-vs-analytic delta + re-check the HeteroInfer staircase trend against ONNXim → `validation/reports/phase1.3/m4_npu_onnxim.json` (simulated; `upgrade.issue_13_silicon = superseded-not-satisfied (independent)`; ONNXim ≠ #13). `tools/plotting/npu_n3_fig.py` → `docs/figures/phase1.3/N3.png`. `docs/report/phase1.3/chapters/N-npu-onnxim.md`. → verify: JSON per-shape delta + #13/ONNXim distinction; every shape an ONNXim hit; figure renders; chapter has sim-vs-analytic.
7. **Docs + commit.** Update `tools/onnxim/README.md` (built on metiscard-Docker, pinned `a1e86296`, the `--models_list` + nested-config + `precision:1` + DRAM-backend + run-inside-image facts, the result), `docs/phase1.3-findings.md` (ONNXim done). secret-scan; commit (exclude `papers/`; local `tools/onnxim/upstream/` gitignored; metiscard checkout remote); push → PR #24. → verify: grep clean; check_phase1_2 / check_phase1_3 / recompose 9.5% green; pushed.

Fallback (no silent fudge): STOP + document + report user (keep `engine='onnxim'` analytic fallback; never fabricate latencies) on any of: (a) Docker build fails; (b) BOTH ramulator2 AND `simple` DRAM backends fail to run (#32); (c) generation/run can't produce a per-shape latency; (d) latency can't be parsed; (e) Docker `ONNXIM_HOME` path/mount mismatch → the Simulator can't find the generated ONNX (so no rows are silently produced). Analytic NPU stays the deliverable; #13 stays superseded; low-value sim-vs-sim.

Outputs: `tools/onnxim/rknpu2_approx.json`; `tools/analysis/npu_onnxim_trace.py` (metiscard) + `build_m4_npu_onnxim.py` + `tools/plotting/npu_n3_fig.py`; `simulated/onnxim/rknpu2_sim_matmul.json`; `validation/reports/phase1.3/m4_npu_onnxim.json`; `docs/figures/phase1.3/N3.png`; `docs/report/phase1.3/chapters/N-npu-onnxim.md`; updated README/findings; commit on `phase-1.3` → PR #24.

## 子計畫 C — CIM-Card 重驗 + edge-CIM 記憶體層 + prefill GEMM 擬合


分支：`phase-1.3`（沿用）；實際執行於 `phase-1.3-cim-card`（隔離 worktree）。執行序：Gate → Spike →（gate）→ Full → Fit → Cross-val → Edge-mem → Honesty/PR。
板存取需使用者授權；Spike 或 Artifactory 不過即 STOP+回報，不進下一段。

> **執行偏差（2026-06-06，已與使用者確認）**：spike 發現大 prefill GEMM 在 v1.6 `axcompile` 編不出（compiler SRAM L1/L2 tiling 牆，非 device-DRAM envelope；單 ~2048×2048 tile 僅 M≤256 可編）。經使用者選定改用 **tile-based prefill**：量 canonical tile 於 M∈{1,64,128,256}、`tile_lat(M)=a+b·M`、full GEMM = `n_tiles×tile_lat`（步驟 9 的 prefill M∈{128..2048} 直接量、步驟 10 的 `matmul_sweep` 群組因此**取消**）。步驟 9/10 的原文僅作歷史紀錄；實況見 `docs/report/phase1.2/chapters/CIM-card.md`。

## G. 前置 Gate（板存取）

1. 使用者授權 `ssh metiscard`（settings 加一條 Bash allow 規則）。 → verify：`ssh metiscard 'echo ok'` 回 `ok`。
2. 確認板在線＋時脈：`ssh metiscard 'source ~/tundergod/voyager-sdk/axelera-env/bin/activate && axdevice -v'`。 → verify：`metis-0:7:0` 在、`clock=800MHz`（非 800 則 `axdevice --set-clock 800`）。

## S. Spike（可行性，~30–60 min）

3. 查編譯器：`ssh metiscard` 跑 `pip list | grep -i axelera`、`which axcompile`、`axcompile --help`。 → verify：記錄 `axelera-devkit`/`axcompile` 有無；存下 `axcompile --help` 的 flag 名稱。
4. 若 `axcompile` 不在：拋棄式 venv 測 Artifactory `python3 -m venv /tmp/axc && /tmp/axc/bin/pip install --extra-index-url https://software.axelera.ai/artifactory/api/pypi/axelera-pypi/simple 'axelera-devkit[all]'`。 → verify：成功或記下 auth/index 錯誤；被擋即 STOP+回報。
5. 改 `characterization/metis_card/run_metis_cim_v16.py:_try_low_level_compile`：先試 `axcompile --input m.onnx --output out --input-shape 1,K,1,M --log-level WARNING`（flag 名先對步驟 3 的 `axcompile --help` 核對），失敗才退舊 `compile`，兩者皆無才回 ABSENT。 → verify：函式先試 `axcompile`；本機乾跑無語法錯。
6. 跑 `run_metis_cim_v16.py --spike`（conv-proxy：square + 2 staircase + 1 prefill M=256）。 → verify：conv proxy 編出 `out/**/model.json`、`axrunmodel` 回 dev/host/system fps（非 ABSENT）。
7. raw-Gemm 探針：寫一個裸 `Gemm` ONNX，`axcompile` 試編一次。 → verify：記錄「編出」或 `ONNXGraphCleanerError`，據此定 Full 用 raw-Gemm 或 conv-proxy。
8. Spike 結論寫回 `validation/reports/phase1.2/cim_card_revalidate.json`（`status: SPIKE_OK|BLOCKED_<reason>`）；回報使用者。 → verify：報告反映 spike 結果；得使用者 OK 才進 Full。

## F. Full 量測（Spike 過 + 使用者 OK 後）

9. 跑 `run_metis_cim_v16.py` full：`alpha13`（13 形狀）+ `prefill` M∈{128,256,512,1024,2048}（gate_up/q_o/down）。 → verify：alpha13 13 點皆有 `dev_gflops`；prefill M≥512 不再 `no_model_json`。
10. `manifest()` 加一個固定小群組 `matmul_sweep`，從 `sweep_matrix.json` 的 `matrix.matmul` 取一組固定形狀（decode-GEMV M=1, K∈{2048,3072,4096} × N∈{1024,2048,8192} + prefill M∈{128,512}）；無新 flag。 → verify：群組形狀皆量到或各自記 `error`。
11. rsync 結果回庫 → `measurements/metis_card/cim_card_revalidate_raw.json`。 → verify：檔在、含 alpha13 + prefill + matmul_sweep 群組。

## P. Prefill GEMM 擬合

12. 新擬合器 `tools/analysis/fit_cim_prefill.py`：在量到的 prefill 點上擬合 `G_eff_prefill(M,K,N)`（含 M amortization）。 → verify：殘差列出；對 M 單調；> 原生 tile 標 extrapolated。
13. prefill 分支接進 `simulator/models/m1_cim_tile.py:dev_lat_us` + `params/m1_cim.json`：M>1 走擬合（M=1 decode 路徑不變）。 → verify：`dev_lat_us(M=128,K=4096,N=14336)` 落在量測 ±擬合容差內（現行線性-M 為 65843µs ≈ 80× 偏離，須消除）。
14. 對 vendor TTFT 交叉驗證：把 prefill-fit 預測**沿 1024-token prompt 組合**後對 vendor `ttft_s=3.794` 比；改 `tools/analysis/recompose_e2e.py` 的 `M_pf=1024` 段改用擬合（取代 implied-TOPS）。 → verify：rel error 列出；prefill 段由 `UNGATED` 升為 gated/有界。

## V. Card 對 Alpha 交叉驗證

15. 跑 `tools/analysis/validate_cim_card.py`：Card 13 點 `dev_gflops` 對 Alpha 13 點（同顆 AIPU、800MHz、無 rescale）算 median/p95 `|rel_diff|`。 → verify：報告轉 `CARD_REVALIDATED`、含 median/p95 + prefill 新點。
16. G_eff 參數決策：`|diff|` 在容差內保留 Alpha 擬合（解凍）；否則對 Card 點重擬。 → verify：`m1_cim.json` provenance 更新（confirmed-on-card 或 re-fit）。

## E. Edge-CIM 記憶體層（從 Alpha + 目標 spec 組，非 Card）

17. 新 spec `simulator/specs/cim_topo_edge.json`：`topology="edge"`（必要，否則 `m2_memory.py` 的 `"topology" in spec` 分支不取）、`on_card_dram=false`、`mem_spec_ref="mem_lpddr5"`、`noc_efficiency`（標 assumption）、`transport`=NoC|PCIe、per-call floor 取 Alpha PCIe 或 0；共用 `m1_cim.json`。 → verify：spec 載入回 dict、含 `topology` + `mem_spec_ref` + 每欄 provenance。
18. 改 `simulator/models/m2_memory.py`：加 `from simulator.specs.loader import load_spec`；topology spec 若 `on_card_dram=false`，`eff_BW_GBs = load_spec(spec["mem_spec_ref"])["peak_GBs"] * spec["noc_efficiency"]`（非 `dram_eff_BW_GBs` 的 None、非 24.2），避免 line 51/77 的 None→AssertionError。 → verify：`MemoryModel(load_spec("cim_topo_edge")).predict(...)` 不報錯、decode BW 隨目標 LPDDR 變、≠ Card 24.2。
19. 註冊新 spec：加進 `validation/contracts/specs.yaml` + `tools/analysis/check_phase1_2.py` 的 `SPECS`，probe 用 `op="stream"`（走新 `eff_BW_GBs` 路徑、非 `op="pcie"`，NoC-default 不會誤 FAIL）。 → verify：`check_phase1_2.py` exit 0 且 `cim_topo_edge` 被 probe。
20. 更新 `docs/report/phase1.2/chapters/CIM-card.md`：加 edge 記憶體牆組成 + provenance（目標 LPDDR + Alpha PCIe + Ramulator，非 Card 24.2）+ CARD_REVALIDATED 結果；移除 DEFERRED_FALLBACK 段。 → verify：章節含 edge provenance + 結果、無 DEFERRED_FALLBACK。

## H. 誠實/回報/PR

21. 更新 `docs/voyager-sdk.md`（§2/§9/§10/§13）：`compile`→`axcompile`、doc 路徑改 `docs/reference/compiler/onnx-support.md`〔路徑待對板上 SDK 核對〕、`axelera-rt`/`axelera-devkit[all]` wheel + **Beta** + Artifactory 權限待證；標 tag。 → verify：相關行更新並標來源。
22. 更新 `validation/reports/phase1.2/cim_card_revalidate.json`：以 axcompile/devkit 發現取代舊「SSH 未授權 / compile 不在」理由，寫最終狀態；保留 `honesty` 欄含 "Alpha 13"+"calibrated"（或 `CARD_REVALIDATED`），勿破壞 `check_phase1_2.py:87-89` gate。 → verify：報告 reason 與現況一致、`check_phase1_2.py` 仍 exit 0。
23. `gh pr create`（`phase-1.3` → `main`）：摘要 + verify 結果。 → verify：PR 開出。

Outputs: `measurements/metis_card/cim_card_revalidate_raw.json`；更新後 `m1_cim.json`(+prefill 參數) 與 `m1_cim_tile.py`；`tools/analysis/fit_cim_prefill.py`；`simulator/specs/cim_topo_edge.json` + `m2_memory.py` edge 分支；更新後 `run_metis_cim_v16.py`、`recompose_e2e.py`、`check_phase1_2.py`、`specs.yaml`；`cim_card_revalidate.json`(`CARD_REVALIDATED`)；`validate_cim_card.py` 報告；CIM-card 章節；`docs/voyager-sdk.md` 更新。
